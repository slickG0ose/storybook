name: Deploy

on:
  push:
    branches: [master]

# Cancel any in-progress run if a new commit lands on master. Last commit wins.
concurrency:
  group: deploy-master
  cancel-in-progress: true

jobs:
  # Placeholder until F5 (demo deploy) lands. F3's deploy stack research doc
  # decides the actual target (Vercel client + Railway/Fly server, vs Vercel
  # everything + Postgres). This stub keeps the workflow file present so
  # required-status-check rules can reference it ahead of time without 404s.
  placeholder:
    name: Placeholder (deploy target TBD via F3 / F5)
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Deploy target not yet configured."
          echo "See docs/deploy-stack-research.md (F3) for the option being chosen,"
          echo "and the F5 task in the foundation plan for the implementation."
