# Vite build-time env. Leave empty for local dev (Vite proxy handles /api).
# For deployed builds, set to the server's origin, e.g.
# VITE_API_BASE_URL=https://storybook-server.onrender.com
VITE_API_BASE_URL=
