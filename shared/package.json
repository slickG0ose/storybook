{
  "name": "@storybook/shared",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Source-only Zod schemas + inferred types shared by client and server. No build step — consumers (Vite, tsx) import .ts directly.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./orders": "./src/orders.ts"
  },
  "dependencies": {
    "zod": "^3.23.8"
  }
}
